# ═══════════════════════════════════════════════════════════════
# GOEIC OFFLINE DEPENDENCIES
# NOTE: torch is installed separately in Dockerfile as CPU-only
#       to avoid downloading the 2GB+ CUDA build.
# ═══════════════════════════════════════════════════════════════

# Core Framework
fastapi==0.115.0
uvicorn[standard]==0.32.0
python-dotenv==1.0.1
pydantic==2.9.2

# Security
bcrypt==4.2.0
python-multipart==0.0.12

# Database
weaviate-client==4.9.3

# Local AI Models
# torch is installed separately in Dockerfile (CPU build)
sentence-transformers==3.3.1
transformers==4.46.3

# Ollama Client
httpx==0.26.0
aiohttp==3.11.7

# Voice
# openai-whisper==20240930   # uncomment to enable voice features
edge-tts==6.1.9

# Web Scraping & Processing
beautifulsoup4==4.12.3
requests==2.32.3
lxml==5.3.0

# Text Processing
langchain-text-splitters==0.3.2

# Excel/Document Processing
pandas==2.2.3
openpyxl==3.1.5
python-docx==1.1.2

# Utilities
tqdm==4.67.1
Pillow==11.0.0

# Arabic text support
arabic-reshaper==3.0.0
python-bidi==0.4.2